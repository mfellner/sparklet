menu "sparkDash validation"
config SPARKDASH_TEST_COMMANDS
    bool "Enable temporary USB test-server and reconnect commands"
    default n
    help
        Validation builds only. Allows a volatile server override over USB and
        station reconnect without changing NVS or production services.
endmenu
